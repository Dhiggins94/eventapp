const Page = () => {
  return <h1> Our Single Event</h1>;
};
export default Page;

// /events/[eventPerCity]/event which can turn into to  something like /events/eventInLonon/funEvent
// we add the [] because it will be ran dyanmically in a folder from the index.js file